https://www.youtube.com/watch?v=N8bRwCvhH6g

Blender 5

press A > X

shift + A > mesh > cylinder > en bas à gauche, ref image creation_window.png > cap fill type > select Nothing

switch to edit mode, tab

press R > X > type 90

press G > Y > move it one unit to the left, ref image move_1unit.png

switch to object mode, tab > shift + D > enter to confirm

2e cylinder: alt + click, pour sélectionner tous les vertices d'un edge (gauche) >  press S, vers l'exterieur (un peu) > press G > Y 

2e cylinder: alt + click, pour sélectionner tous les vertices d'un edge (droite) > press G > Y, un peu > press S, vers l'intérieur

2e cylinder: ctrl + R, tout en survolant la partie où je souhaite ajouter des loops > je scroll up pour augmenter le nombre de loops, 3 > enter to confirm

2e cylinder: alt + click, pour sélectionner tous les vertices d'un edge (le plus à droite droite) > press S, vers l'intérieur

2e cylinder: alt + click, pour sélectionner tous les vertices d'un edge (le 2e en partant de la droite) > press S, vers l'intérieur

select le 1er cylinder, ctrl + R, tout en survolant la partie où je souhaite ajouter des loops > je scroll up pour augmenter le nombre de loops, 4 > enter to confirm

1er cylinder: alt + click, pour sélectionner tous les vertices d'un edge (le plus à droite droite) > press S, vers l'intérieur

switch to object mode > select object (1er cylinder) > click droit > shade smooth

switch to object mode > select object (2e cylinder) > click droit > shade smooth

select cylinder 1 > en haut a gauche, file menu > export > gltf > include menu à droite > limited to > checked selected object

select cylinder 2 > en haut a gauche, file menu > export > gltf > include menu à droite > limited to > checked selected object

////////////////////////

Godot 4.5.1

add node 3D > add GPUparticule3D node > inspector > process material > new particle process material

cylinder1: drag n drop model, dans le dossier models > click droit > new scene inherited > select mesh > inspector > mesh > fleche à droite de la mesh, liste déroulante > rendre unique > save as, select folder

cylinder2: drag n drop model, dans le dossier models > click droit > new scene inherited > select mesh > inspector > mesh > fleche à droite de la mesh, liste déroulante > rendre unique > save as, select folder

select GPUparticule3D node > inspector > draw passes > pass > charger > select mesh, cylinder 1

select GPUparticule3D node > inspector > amount == 1

select GPUparticule3D node > inspector > time > lifetime == 2

select GPUparticule3D node > inspector > process material > click on > acceleration > gravity > y == 0

select GPUparticule3D node > inspector > process material > click on > display > scale > scale curve > curveXYZTexture > click on
curve Z () > select le graph > select le 1er point à gauche (start), set it to 0 > max value == 8 > select le 2e point à droite (end), set it to 8
ref image curveZ.png

select GPUparticule3D node > inspector > geometry > material override > new shader material > click on > shader
 new shader > visual shader > select folder > create > click on
mode > cull > disabled
flags > unshaded, checked

COULEUR SHADER
db click sur le fichier visual shader > en bas au niveau du terminal, onglet shader editor > select fragment ref image visual_shader_fragment.png

click droit, pour ajouter un nouveau noeud > select (search input) color > connect node to albedo (output)

select GPUparticule3D node > inspector > process material > click on > display > color curve > color

NOISE TEXTURE
db click sur le fichier visual shader > en bas au niveau du terminal, onglet shader editor > drag n drop texture image

create new node vector4Decompose > texture2d node (color) link to vector4Decompose node (vec)

create new node power (function) > vector4Decompose node (x) link to power node (a) > b == 1.5

power node (op) link to output node (alpha)

TEXTURE EN MOVEMENT
create new node, UVPanning

create new node, time (input)

create new node, multiply (operator > vec2)

time node (uv) link to multiply node (a) > multiply node (op) link to UVPanning node (offset) > UVPanning node (uv) link to texture2d node (uv)

select multiply node > b: x=2;y=1 

COLOR OPACITY
create new node vector4Decompose

create new node multiply (operator > float)

input color node link to vector4Decompose node (vec) > vector4Decompose node (w) link to multiply node (a)

power node (op) link to multiply node (b) > multiply node (op) link to output node (alpha)

PARAMETER
create new node floatparameter > "Texture_power" > defaul value enable, checked > default value = 1.5
floatparameter node link to power node (b)

create new node vector2parameter > "Speed" > defaul value enable, checked > default value = 1.5
vector2parameter node (vector) link to multiply node (b)

delete texture2D node
create new texture2Doarameter node > "Energy_texture"

create texture2D node > liste deroulante, texture change it to sample port (texture type)
texture2Doarameter node (sample2D) link to texture2D node (sample2D)
UVPanning node (uv) link to texture2d node (uv)
texture2d node (color) link to vector4Decompose node (vec)

scene > select GPUParticle3D node > inspector > material override > click on > shader parameters > drag n drop texture to "Energy texture" slot
texture == nois 9 ; pow == 1.3 ; speed == 2;1

select GPUParticle3D node > inspector > process material > click on > display > color curves > color > click on > raw tab, ref image raw_tab.png

****

add GPUparticule3D node > inspector > process material > new particle process material

select GPUparticule3D node > inspector > draw passes > pass > charger > select mesh, cylinder 2

select GPUparticule3D node > inspector > amount == 1

select GPUparticule3D node > inspector > time > lifetime == 1.5

select GPUparticule3D node > inspector > process material > click on > acceleration > gravity > y == 0

select GPUparticule3D node > inspector > geometry > material override > new shader material > click on > shader > drag n drop "s_dynamic_shader.tres"
shader parameter > texture == nois 4 ; pow == 1.5 ; speed == 1.5;2

select GPUParticle3D node > inspector > process material > click on > display > color curves > color > click on > raw tab, ref image raw_tab.png

BORDERLESS TEXTURE
db click sur le fichier visual shader > en bas au niveau du terminal, onglet shader editor > select fragment

create texture2D node > click on, slot empty ref image gradient_texture2d.png > click on gradient_texture2d > inspector > fill > to > x=0;y=1

create vector4Decompose node

create pow (float) node

create floatparameter node > "gradient power" > default value, checked > default value == 1

create node multiply (float)

texture2d node (color) link to vector4Decompose node (vec)
vector4Decompose node (x) link to pow node (a)
floatparameter node link to pow node (b)
pow node (op) link to multiply node (b)
multiply node (op) link to multiply node (a)
multiply node (op) [le node qui a permis de modifier color opacity] link to output node (alpha)